image: rust:latest

before_script:
  - apt-get update
  - apt-get install -y libasound2-dev

stages:
  - build
  - test

build:
  stage: build
  script:
    - cargo build

test:
  stage: test
  script:
    - cargo build
    - cd tests
    - python3 test.py
